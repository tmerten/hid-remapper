# Experiment: do not auto-initiate the LE PHY update (prefer 2M) after
# connecting. The Zephyr host sends it right after connect, alongside pairing.
# The Pi Zero W (BT 4.1, no 2M PHY) that paired fine cannot, and Linux does
# not request 2M by default.
CONFIG_BT_AUTO_PHY_UPDATE=n
